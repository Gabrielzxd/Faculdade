tipos
    PNo = ponteiro para TNo
    TNo = registro
            info: real
            prox: PNo
var
    função QuantidadeXNaLista(x: real; l: PNo): inteiro
    var
        qnt: inteiro
    início
        qnt <- 0
        enquanto l != Nulo faça
            se l^.info = x então
                qnt <- qnt + 1
            fim se
            l <- l^.prox
        fim enquanto
        retorne qnt
    fim função