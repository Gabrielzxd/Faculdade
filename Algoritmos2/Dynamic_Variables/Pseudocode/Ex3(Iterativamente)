tipos
    PNo = ponteiro para TNo
    TNo = registro
            info: real
            prox: PNo
        fim registro
var
    função VerificaSeElementoEstaNaLista(x: real; l: PNo): lógico
    início
        enquanto l != Nulo faça 
            se l^.info = x então
                retorne verdadeiro
            fim se
            l <- l^.prox
        fim enquanto
        retorne falso
    fim função