tipos
    PNo = ponteiro para TNo
    TNo = registro
            info: real
            prox: PNo
        fim registro
var
    função VerificaSeElementosAparecemJuntos(x, y: real; l: PNo): lógico
    início
        enquanto l != Nulo e l^.prox != Nulo faça
            se l^.info = x e l^.prox^.info = y então
                retorne verdadeiro
            fim se
            l <- l^.prox
        fim enquanto
        retorne falso
    fim função