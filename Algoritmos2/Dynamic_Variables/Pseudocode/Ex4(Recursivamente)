tipos
    PNo = ponteiro para TNo
    TNo = registro
            info: real
            prox: PNo
var
    função QuantidadeXNaLista(x: real; l: PNo): inteiro
    var
        qnt: inteiro
    início
        se l = Nulo então
            retorne 0
        fim se
        se l^.info = x então
            retorne 1 + QuantidadeXNaLista(x, l^.prox)
        fim se
        retorne QuantidadeXNaLista(x, l^.prox)
    fim função