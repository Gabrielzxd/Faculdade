função MaiorValor(flag: real): real
var
    maior, num: real
início
    leia(num)
    se num = flag então
        retorne num
    fim se
    maior <- MaiorValor(flag)
    se num > maior ou maior = flag então
        retorne num
    fim se
fim função  